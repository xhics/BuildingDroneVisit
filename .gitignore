# Secrets — jamais committés (plan directeur §6)

id_ed25519
id_ed25519.pub
runpod_gen
runpod_gen.pub

# Espaces de travail et artefacts
work/
*.tmp

# Cache local des appels externes (complément §3.2)
.cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Artefacts GPU locaux et simulations RunPod (volumineux, reproductibles)
runpod/results/
runpod/bundle-candidate/
runpod/dry-run-check/
runpod/welcominns-runpod-upload.tar.gz
welcominns-workspace.tar.gz
welcominns-workspace.tar.gz.part-*

# macOS
.DS_Store

# Données géospatiales brutes — volumineuses, reproductibles depuis la provenance
*.LAZ
*.laz

# Sorties du mode simple (simple_mode/) — régénérables depuis une adresse
/out.png
/out.md
/out.illustration.png
simple_mode/out*
video_out/
